services: 
  sqlite3:
    image: nouchka/sqlite3:latest
    stdin_open: true
    tty: true
    volumes:
      - ./db/:/root/db/
  web: 
    build:
     context: app
     target: builder
    ports: 
      - '5000:5000'
    volumes:
      - ./db/:./instance/